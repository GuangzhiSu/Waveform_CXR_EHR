# Baseline2: ECG-only → Oxygenation

Single modality: ECG waveform only predicts oxygenation at matched timepoint.

**Run:**
```bash
cd /work/gs285/Waveform_CXR_EHR
sbatch baseline2/run_baseline2.sh
```

Or interactive: `cd baseline2 && ./run_baseline2.sh`
