# Baseline3: CXR-only → Oxygenation

Single modality: CXR image only predicts oxygenation at matched timepoint.

**Run:**
```bash
cd /work/gs285/Waveform_CXR_EHR
sbatch baseline3/run_baseline3.sh
```

Or interactive: `cd baseline3 && ./run_baseline3.sh`
